/**
 * Plugin constants (hardcoded configuration) for shared code.
 */
export default class Fl64_Habr_Web_Shared_Defaults {
    NAME = '@flancer64/habr_teqfw_web';

    WEB_LOAD_PLUGINS = '/load/plugins';

    constructor() {
        // MAIN FUNCTIONALITY
        Object.freeze(this);
    }
}
